name: Show Directory Structure

on:
  push:
    branches:
      - main 

jobs:
  show_directory_structure:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Show Directory Structure
        run: tree
